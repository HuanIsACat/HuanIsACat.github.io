<!DOCTYPE html>
<html lang="zh">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>{{ title }} · Dream Codex</title>
  
  <link href="https://fonts.googleapis.com/css2?family=Alice&family=Philosopher:ital,wght@0,400;0,700;1,400&display=swap" rel="stylesheet">
  
  <style>
    :root {
      --bg: radial-gradient(circle at top, #2e1b47, #120a1f 90%);
      --card: rgba(45, 30, 70, 0.6); 
      --glow: rgba(180, 120, 255, 0.3);
      --border: rgba(200, 160, 255, 0.2);
      --text-main: #eeeeee;
      --text-dim: rgba(238, 238, 238, 0.7);
      --accent: #bd93f9;
    }

    /* 基础样式继承 */
    * { box-sizing: border-box; }

    body {
      margin: 0;
      min-height: 100vh;
      background: var(--bg);
      background-attachment: fixed;
      color: var(--text-main);
      font-family: 'Alice', "Yu Mincho", "SimSun", serif;
      line-height: 1.8;
      overflow-x: hidden;
    }

    /* 入场动画 */
    @keyframes fadeIn {
      from { opacity: 0; transform: translateY(20px); filter: blur(10px); }
      to { opacity: 1; transform: translateY(0); filter: blur(0); }
    }

    header {
      padding: 60px 5vw 20px;
      max-width: 800px;
      margin: 0 auto;
      text-align: center;
      animation: fadeIn 1.5s ease-out;
    }

    .breadcrumb {
      font-family: 'Philosopher', sans-serif;
      font-size: 0.9em;
      letter-spacing: 2px;
      margin-bottom: 20px;
      opacity: 0.6;
    }

    .breadcrumb a {
      color: var(--accent);
      text-decoration: none;
      transition: opacity 0.3s;
    }

    .breadcrumb a:hover { opacity: 1; }

    h1 {
      font-family: 'Philosopher', sans-serif;
      font-size: 2.8em;
      color: var(--accent);
      text-shadow: 0 0 15px rgba(189, 147, 249, 0.3);
      margin: 10px 0;
    }

    .entry-meta {
      font-size: 1em;
      color: var(--text-dim);
      margin-top: 15px;
      letter-spacing: 1px;
      border-bottom: 1px solid var(--border);
      padding-bottom: 30px;
      display: inline-block;
      width: 100%;
    }

    /* 正文容器 */
    main {
      padding: 0 5vw 100px;
      max-width: 800px;
      margin: 0 auto;
      animation: fadeIn 2s ease-out;
    }

    .dream-body {
      background: var(--card);
      backdrop-filter: blur(10px);
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 60px;
      box-shadow: 0 20px 50px rgba(0,0,0,0.5);
      position: relative;
    }

    /* 文学感修饰：首字下沉 */
    .dream-body p:first-of-type::first-letter {
      float: left;
      font-size: 3.5em;
      line-height: 1;
      padding-right: 12px;
      color: var(--accent);
      font-family: 'Philosopher', serif;
    }

    .dream-text {
      font-size: 1.15em;
      color: rgba(255, 255, 255, 0.9);
      margin-bottom: 30px;
      text-align: justify;
    }

    /* 章节分割线 */
    .divider {
      text-align: center;
      margin: 50px 0;
      opacity: 0.4;
      color: var(--accent);
      font-size: 1.5em;
    }

    /* 标注与感悟 */
    .reflection {
      font-style: italic;
      border-left: 2px solid var(--accent);
      padding-left: 25px;
      margin: 40px 0;
      color: var(--text-dim);
      font-size: 1.05em;
    }

    /* 标签区域 */
    .tags {
      margin-top: 60px;
      padding-top: 20px;
      border-top: 1px solid var(--border);
    }

    .tag {
      display: inline-block;
      padding: 4px 15px;
      background: rgba(189, 147, 249, 0.1);
      border: 1px solid var(--border);
      border-radius: 20px;
      font-size: 0.85em;
      margin-right: 10px;
      color: var(--accent);
    }

    /* 底部导航 */
    .nav-footer {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-top: 50px;
    }

    .back-btn {
      display: inline-block;
      text-decoration: none;
      font-family: 'Philosopher', sans-serif;
      color: var(--accent);
      font-size: 0.95em;
      padding: 10px 25px;
      border: 1px solid var(--border);
      border-radius: 50px;
      background: rgba(60, 40, 90, 0.3);
      transition: all 0.4s ease;
    }

    .back-btn:hover {
      background: rgba(189, 147, 249, 0.2);
      border-color: var(--accent);
      box-shadow: 0 0 15px var(--glow);
    }

    footer {
      text-align: center;
      padding: 60px 5vw;
      opacity: 0.4;
      font-size: 0.85em;
      letter-spacing: 1px;
    }

    /* 响应式适配 */
    @media (max-width: 600px) {
      .dream-body { padding: 30px 20px; }
      h1 { font-size: 2em; }
    }
  </style>
</head>
<body>

<header>
  <div class="breadcrumb">
    <a href="{{ '/index.html' | url }}">DREAM CODEX</a> / ENTRY
  </div>
  <h1>{{ title }}</h1>
  <div class="entry-meta">
    🗓 {{ date }} &nbsp; | &nbsp; 🎭 {{ emotion }}
  </div>
</header>

<main>
  <article class="dream-body">
    {{ content | safe }}
    
    <div class="tags">
      {% for tag in tags %}
      <span class="tag"># {{ tag }}</span>
      {% endfor %}
    </div>
  </article>

  <div class="nav-footer">
    <a href="{{ '/index.html' | url }}" class="back-btn">← 返回幻象集 (Return)</a>
    <span style="opacity: 0.3; font-size: 0.8em;">#编号: {{ id }}</span>
  </div>
</main>

<footer>
  - Recorded in the subconscious timeline -
</footer>

</body>
</html>
